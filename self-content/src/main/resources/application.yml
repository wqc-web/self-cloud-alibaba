server:
  port: 8091
  servlet:
    context-path: /content
spring:
  profiles:
    active: dev
# actuator
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        #显示所有端点
        include: "*"