server:
  port: 8081
  servlet:
    context-path: /user
spring:
  profiles:
    active: dev
# actuator
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        #显示所有端点
        include: "*"