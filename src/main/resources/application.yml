server:
  port: 8080
  servlet:
    context-path: /cloud
spring:
  profiles:
    active: dev
# actuator
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        #显示所有端点
        include: "*"